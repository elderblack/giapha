/** Nguồn dữ liệu: `apps/web/src/content/devPlan.ts` — chỉ chỉnh ở đó. */
export type { PlanPhase, PlanTask } from '@giapha/dev-plan'
export {
  buildShippedDefaults,
  countTasks,
  DEVELOPMENT_PLAN,
  getAllTaskIds,
  loadPlanProgress,
  PLAN_PROGRESS_STORAGE_KEY,
  savePlanProgress,
} from '@giapha/dev-plan'
